# Keep shell scripts and systemd units as LF regardless of platform.
# Windows checkouts otherwise get CRLF, which makes bash fail at runtime
# with "bad interpreter: /usr/bin/env bash^M".
*.sh              text eol=lf
*.service         text eol=lf
*.timer           text eol=lf
